jQuery(document).ready(function ($) {
  // Move the custom card to the end of the #side-sortables section in WooCommerce order detail page
  $("#pricemyway-ambassador-card").appendTo("#side-sortables");
  $("#pricemyway-ambassador-card").removeClass("hidden");
});
